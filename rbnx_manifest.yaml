package:
  name: navigation2_base
  version: 0.0.1
  description: navigation2_base
  maintainer: zhengwu
  maintainer_email: wuzheng_study@163.com
  license: Apache-2.0
  build_script: rbnx/install.sh

primitives:
  - name: prm::base.nav  # 标准能力名称 (如 cap::grasp.move)
    version: 0.0.1-impl-alpha
    start_script: rbnx/start.sh    # 启动脚本路径 (相对于 package 根目录，如 "rbnx/start_cap.sh")
    stop_script: rbnx/stop.sh     # 停止脚本路径 (相对于 package 根目录，如 "rbnx/stop_cap.sh")
    input_schema: '{}'              
    output_schema: '{}'  
  
skills:
  # no skills
